# Secrets — never commit these
.env
# local provider config with API key
opencode.json

# Claude / opencode local config
.opencode/
AGENTS.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# Runtime artifacts
workspace/
codex_bridge_debug.log
*.log
agent/data/memory/
# persistent langgraph checkpoints (run state — resumable but not source)
agent/data/*.sqlite
agent/data/*.sqlite-*

# IDE / OS
.vscode/
.idea/
*.swp
Thumbs.db